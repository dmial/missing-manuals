# MCP, REST API и протоколы: как AI-агенты общаются с внешним миром

Заметки из сессии глубокого разбора MCP, REST, RPC, транспортных протоколов и практических решений о том, когда что использовать.

---

## 1. Зачем всё это нужно

LLM-агенту нужно взаимодействовать с внешним миром: читать почту, смотреть календарь, работать с файлами. Есть два принципиально разных подхода к организации этого взаимодействия: REST API и MCP.

---

## 2. REST API — классический способ

REST (Representational State Transfer) — архитектурный стиль для взаимодействия клиент-сервер через HTTP. Мыслит **ресурсами** (существительными):

```
GET    /emails/42        — получить письмо
POST   /emails           — отправить письмо
DELETE /emails/42        — удалить письмо
```

В контексте AI-агента: твой Python-код делает HTTP-запросы, парсит JSON-ответы, передаёт результат модели:

```
LLM → "хочу прочитать почту" → твой код → HTTP GET /inbox → JSON ← парсинг → текст для LLM
```

Ключевое: **твой код** — посредник между LLM и API. Ты пишешь HTTP-клиент, знаешь эндпоинты, формат ответов, авторизацию.

---

## 3. MCP — протокол для AI-агентов

MCP (Model Context Protocol) — стандарт, позволяющий LLM вызывать инструменты (tools) через унифицированный интерфейс. Вместо написания HTTP-клиента для каждого API, MCP-сервер описывает свои возможности, а LLM сам выбирает и вызывает нужные.

### Главный инсайт: LLM не знает про MCP

MCP — это протокол **между хостом и сервером**, а не между моделью и сервером:

```
┌─────────┐     tool_use/tool_result      ┌──────────┐     JSON-RPC      ┌────────────┐
│   LLM   │  ◄──────────────────────────►  │   Хост   │  ◄─────────────►  │ MCP-сервер │
│         │   (стандартный API модели)      │(Claude   │   (stdio/SSE)    │(Read AI,   │
│         │   модель НЕ знает про MCP      │ Code)    │                   │ GitHub...) │
└─────────┘                                └──────────┘                   └────────────┘
```

Хост (например, Claude Code) делает всю работу:

1. Спрашивает MCP-сервер: «какие у тебя tools?»
2. Получает список с названиями и JSON Schema параметров
3. Превращает их в **обычные tool definitions** для модели
4. Когда модель делает tool_call — транслирует его в JSON-RPC вызов к MCP-серверу
5. Ответ сервера конвертирует обратно в tool_result для модели

**Модель думает, что вызывает обычные tools.** Она не знает, что за ними стоит MCP-сервер, REST API, база данных или файл на диске. Поэтому даже старая модель, работавшая до изобретения MCP, прекрасно бы с ним работала — при условии, что хост умеет конвертировать MCP tools в формат tool definitions этой модели.

---

## 4. Протоколы внутри протоколов

### 4.1. HTTP — транспорт, а не REST

HTTP — это транспортный протокол. REST — одно из соглашений о том, **как** использовать HTTP. Это разные вещи:

- **HTTP** — дорога
- **REST** — правило «грузовики по правой полосе, легковые по левой»
- **JSON-RPC поверх HTTP** — другое правило: «все по одной полосе, но на борту написано кто ты»

POST — это HTTP-метод, а не REST. И MCP, и REST используют HTTP, но по-разному.

### 4.2. HTTP: текстовый или бинарный?

Гибридный. **Заголовки — текст, тело — что угодно:**

```http
POST /upload HTTP/1.1              ← текст
Host: example.com                  ← текст
Content-Type: image/jpeg           ← текст (описывает бинарное тело)
Content-Length: 482901             ← текст
                                   ← пустая строка = конец заголовков
ÿØÿà..JFIF.....ÿÛ.C...          ← БИНАРНЫЕ БАЙТЫ JPEG
```

Примеры бинарных тел по HTTP: скачивание файлов (`application/octet-stream`), видео (`video/mp4`), gRPC (`application/grpc`), PDF. Когда YouTube стримит видео — это сотни HTTP GET-запросов, каждый из которых возвращает маленький бинарный сегмент (2-10 секунд видео в нужном качестве).

> HTTP/2 и HTTP/3 и заголовки передают в бинарном формате (сжатые), но логически это тот же HTTP.

### 4.3. Методы HTTP

9 стандартных методов (RFC 9110):

| Метод | Для чего | CRUD |
|---|---|---|
| **GET** | Получить ресурс | Read |
| **POST** | Создать / отправить данные | Create |
| **PUT** | Заменить ресурс целиком | Update (полный) |
| **PATCH** | Частично изменить | Update (частичный) |
| **DELETE** | Удалить | Delete |
| **HEAD** | Как GET, но без тела (только заголовки) | — |
| **OPTIONS** | Узнать возможности сервера (CORS preflight) | — |
| **TRACE** | Эхо для отладки | — |
| **CONNECT** | Установить туннель (HTTPS через прокси) | — |

На практике 95% — GET и POST. REST добавляет PUT/PATCH/DELETE. TRACE и CONNECT ты, скорее всего, никогда не вызовешь сам.

---

## 5. RPC — вызов удалённых функций

### 5.1. Идея

RPC (Remote Procedure Call) — вызов функции, которая выполняется где-то ещё. Выглядит как обычный вызов, но под капотом — сетевой запрос:

```python
result = calculator.add(2, 3)           # локальный вызов
result = remote_calculator.add(2, 3)    # RPC — выполняется на сервере
```

**REST мыслит существительными** (ресурсами): `GET /users/42`. **RPC мыслит глаголами** (вызовами): `call getUser(42)`.

### 5.2. JSON-RPC — конкретная реализация

Это стандарт **формы** (спецификация на одну страницу: jsonrpc.org). Фиксирует структуру JSON-сообщений:

**Запрос:**
```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "tools/call",
  "params": { "name": "read_inbox", "arguments": {} }
}
```

**Ответ:**
```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": { "content": [{"type": "text", "text": "3 письма"}] }
}
```

**Ошибка:**
```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "error": { "code": -32601, "message": "Method not found" }
}
```

Стандарт определяет только конверт (`jsonrpc`, `id`, `method`, `params`, `result`, `error`). Какие `method` бывают и что в `params` — определяет конкретный протокол. MCP определяет `tools/call`, `tools/list`. Ethereum определяет `eth_getBalance`, `eth_sendTransaction`.

### 5.3. JSON-RPC — текстовый. Почему не бинарный?

Осознанный выбор: **простота важнее производительности**. MCP передаёт небольшие сообщения (описания tools, текстовые результаты), не гигабайты. Выигрыш от бинарного формата незаметен, а сложность реализации вырастет.

Для сравнения: gRPC (бинарный, Protocol Buffers) выбирают, когда микросервисы обмениваются миллионами сообщений в секунду. MCP — это десятки вызовов за сессию. JSON читается глазами, парсер есть в каждом языке, порог входа минимальный.

### 5.4. Семейство RPC-технологий

| Технология | Формат данных | Транспорт | Где используется | Статус |
|---|---|---|---|---|
| **JSON-RPC** | JSON (текст) | Любой | MCP, LSP, Ethereum | Актуален |
| **gRPC** | Protobuf (бинарный) | HTTP/2 | Микросервисы, Kubernetes | Очень актуален |
| **tRPC** | JSON (текст) | HTTP | TypeScript full-stack | Популярен |
| **XML-RPC / SOAP** | XML (текст) | HTTP | Энтерпрайз 2000-х | Устарел |

RPC вездесущ, но часто скрыт: LSP (подсветка кода в VSCode) — это JSON-RPC, GraphQL — по сути RPC с единым эндпоинтом. Веб-разработчики часто не сталкиваются с RPC напрямую, потому что REST стал «дефолтом» в вебе. Но в инфраструктуре и тулинге RPC живее всех живых.

---

## 6. Транспорты MCP

### 6.1. Матрёшка протоколов

```
REST API:          запрос     →  HTTP           →  TCP  →  интернет
                   (JSON)

MCP (локальный):   JSON-RPC   →  stdio (stdin/stdout)   →  локальный процесс

MCP (удалённый):   JSON-RPC   →  Streamable HTTP (POST + SSE)  →  TCP  →  интернет
```

MCP = JSON-RPC, завёрнутый в один из двух транспортов:

| Транспорт | Когда | Как работает |
|---|---|---|
| **stdio** | MCP-сервер локальный | Хост запускает процесс, пишет JSON в stdin, читает из stdout |
| **Streamable HTTP** | Удалённый сервер | Клиент шлёт POST, сервер отвечает JSON или SSE-потоком |

### 6.2. SSE — Server-Sent Events

SSE — это часть спецификации HTML (WHATWG). Простой механизм: сервер держит HTTP-соединение открытым и шлёт текстовые события.

Как выглядит в сниффере:

```http
HTTP/1.1 200 OK
Content-Type: text/event-stream
Cache-Control: no-cache
Connection: keep-alive

data: {"jsonrpc":"2.0","id":1,"result":{"content":[{"type":"text","text":"часть 1"}]}}

data: {"jsonrpc":"2.0","id":2,"result":{"content":[{"type":"text","text":"часть 2"}]}}
```

Каждое сообщение — `data: ...`, разделение — пустая строка. SSE — **однонаправленный** (сервер → клиент), в отличие от WebSocket. Используется для стриминга ChatGPT/Claude, нотификаций в реальном времени, биржевых котировок, CI/CD логов.

### 6.3. SSE vs WebSocket

| | SSE | WebSocket |
|---|---|---|
| Направление | Сервер → клиент | Обе стороны |
| Протокол | Остаётся HTTP на всём протяжении | HTTP только для рукопожатия, потом переключается на бинарный протокол WebSocket |
| Формат | Текст (`data: ...`) | Бинарные фреймы |
| Пример | MCP, стриминг LLM-ответов | Онлайн-игры, чаты, трейдинг |

WebSocket начинается как HTTP (`GET` + заголовок `Upgrade: websocket`), сервер отвечает `101 Switching Protocols`, и дальше это уже **не HTTP** — бинарные фреймы в обе стороны.

### 6.4. Эволюция транспорта MCP

**Старый (2024, deprecated):** два эндпоинта — `GET /sse` для приёма событий, `POST /message` для отправки запросов.

**Новый (2025, Streamable HTTP):** один эндпоинт — `POST /mcp`, сервер сам решает, ответить обычным JSON или SSE-потоком.

---

## 7. Что возвращает MCP-сервер

JSON-RPC сообщения. Полезная нагрузка внутри — массив блоков `content`:

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "content": [
      { "type": "text", "text": "У вас 3 непрочитанных письма..." },
      { "type": "image", "data": "/9j/4AAQ...", "mimeType": "image/jpeg" }
    ]
  }
}
```

Типы блоков:
- `text` — текст (самый частый)
- `image` — base64-картинка
- `resource` — ссылка на MCP-ресурс

### 7.1. base64: бинарные данные в текстовом протоколе

base64 — способ записать бинарные данные (байты JPEG/PNG) в виде ASCII-текста. Каждые 3 байта → 4 символа (из алфавита `A-Z`, `a-z`, `0-9`, `+`, `/`). Размер +33%.

Зачем: JSON не умеет хранить бинарные данные, а весь MCP — текстовый. Это стандартный костыль для текстовых протоколов. В HTTP без JSON-обёртки есть альтернативы:

- **Отдельный URL** — загрузить файл на CDN, передать ссылку (так работают Slack, Telegram)
- **multipart/form-data** — текст и бинарные данные рядом в одном запросе, разделённые boundary-строкой (так работает `<input type="file">` в браузере)

В MCP — только base64 внутри JSON. Это осознанное ограничение: MCP не предназначен для передачи гигабайтных файлов.

---

## 8. Архитектура MCP: хост, клиент, сервер

```
┌─────────────────────────────────────────┐
│                 Хост                     │
│  (Claude Code / Claude Desktop / IDE)    │
│                                          │
│  ┌─────────────┐    ┌─────────────────┐  │
│  │  LLM-слой   │    │  MCP-клиент(ы)  │  │
│  │  (вызов     │◄──►│  (библиотека,   │  │
│  │   Claude    │    │   поддерживает  │  │
│  │   API)      │    │   JSON-RPC)     │  │
│  └─────────────┘    └────────┬────────┘  │
│                              │            │
└──────────────────────────────┼────────────┘
                               │ stdio / HTTP
                    ┌──────────▼──────────┐
                    │    MCP-серверы       │
                    │  (Read AI, GitHub…)  │
                    └─────────────────────┘
```

- **Хост** — приложение целиком (Claude Code)
- **MCP-клиент** — компонент внутри хоста, умеющий общаться по JSON-RPC
- **MCP-сервер** — внешний процесс/сервис, предоставляющий tools

Один хост содержит **несколько инстансов** MCP-клиента — по одному на каждый подключённый сервер. Код класса один, но каждый инстанс поддерживает своё соединение (stdio pipe или HTTP). Как три вкладки браузера — один код `fetch`, но три независимых HTTP-соединения.

MCP-клиент **не специфичен для сервера**. Протокол стандартизирован: один и тот же клиент внутри Claude Code общается с Read AI, GitHub и любым другим MCP-сервером. Anthropic предоставляет референсные SDK (Python: `mcp`, TypeScript: `@modelcontextprotocol/sdk`), но можно написать с нуля по спецификации.

---

## 9. Когда REST API, когда MCP

Ключевой вопрос: **кто принимает решение, какой инструмент вызвать?**

### REST API — когда логика фиксирована

Ты точно знаешь шаги и пишешь их в коде:

```python
# Ежедневный отчёт: шаги известны заранее
async def daily_report():
    meetings = await readai.get("/meetings", params={"date": yesterday})
    for m in meetings:
        transcript = await readai.get(f"/meetings/{m['id']}/transcript")
        summary = await claude.summarize(transcript)
        await slack.post(channel, summary)
```

Скрипты, cron-задачи, ETL-пайплайны, интеграции — REST API напрямую.

### MCP — когда решение принимает LLM

Множество возможных запросов слишком велико для `if/else`:

```
Пользователь: "Что решили по бюджету на вчерашнем созвоне с финансами?"

LLM должна сама:
1. Найти вчерашние встречи → list_meetings
2. Отфильтровать по теме "финансы"
3. Достать транскрипт → get_meeting_by_id
4. Найти обсуждение бюджета
5. Сформулировать ответ
```

Чат-боты, ассистенты, агенты — MCP (или tool_use).

### Гибридный вариант — самый частый

В одном приложении оба подхода:

| Компонент | Подход |
|---|---|
| Cron-задача (ежедневный отчёт) | REST API напрямую |
| Чат-интерфейс (вопросы пользователя) | LLM + MCP |
| Дашборд (статистика) | REST API напрямую |

---

## 10. Кто пишет MCP-серверы

Если поставщик дал только REST API — MCP-сервер пишешь **ты** (или используешь написанный сообществом):

| Кто написал | Пример |
|---|---|
| **Поставщик** | Read AI, GitHub, Stripe — дают готовый MCP-сервер |
| **Сообщество** | npm/pip пакеты вроде `mcp-server-postgres` |
| **Ты** | Если API есть, а MCP-сервер никто не написал |

Написать MCP-сервер — это обёртка над REST API:

```python
from mcp.server.fastmcp import FastMCP

mcp = FastMCP("my-readai")

@mcp.tool()
async def list_meetings(date: str) -> str:
    """List meetings for a given date."""
    resp = await httpx.get(
        "https://api.read.ai/meetings",
        params={"date": date},
        headers={"Authorization": f"Bearer {TOKEN}"}
    )
    return resp.text
```

---

## 11. Когда MCP — оверкилл

### Пример: ToolRegistry вместо MCP

Если ты пишешь агента, который использует Claude API и набор tools известен заранее, можно обойтись без MCP. Пример — паттерн ToolRegistry:

```
Что делает MCP-клиент + сервер:          Что делает ToolRegistry:

Сервер хранит определения tools           get_definitions() → JSON schemas
Сервер содержит реализации tools          execute(name, args) → результат
Клиент запрашивает список tools           get_definitions() — тот же вызов
Клиент шлёт JSON-RPC серверу              execute() — прямой вызов функции
Клиент возвращает результат LLM           Возвращает результат LLM
```

Те же шаги, но без JSON-RPC и сетевого транспорта — потому что клиент и сервер живут в одном процессе. Замена mock → production API — замена одного модуля.

### Когда MCP оправдан

| Сценарий | MCP нужен? |
|---|---|
| Один агент, один набор tools | Нет — ToolRegistry достаточно |
| 10 агентов используют одни tools | Да — один сервер, 10 клиентов |
| Tools подключаются в рантайме из разных источников | Да — динамическое обнаружение |
| Ты хочешь дать свои tools другим | Да — стандартный протокол |

MCP решает проблему **интероперабельности**: один сервер — много клиентов, или один клиент — много серверов. Если у тебя один клиент и один сервер — MCP как USB-хаб для одного устройства.

Даже если поставщик предложит MCP-сервер, а у тебя уже работает прямая интеграция через ToolRegistry — **не обязательно переключаться**. Свой адаптер даёт больше контроля: логирование, кэширование, retry, подмена отдельных tools. На конкурсе важна скорость и контроль, а не красота архитектуры.
